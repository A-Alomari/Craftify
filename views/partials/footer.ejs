<footer class="w-full bg-surface-container-lowest text-on-surface border-t border-outline-variant/20">
    <div class="max-w-7xl mx-auto px-10 py-16 grid grid-cols-1 md:grid-cols-4 gap-10 font-['Inter'] text-sm leading-relaxed text-secondary">
        <!-- Column 1: Brand -->
        <div class="space-y-6">
            <a class="font-['Noto_Serif'] text-2xl font-bold text-on-surface tracking-tight" href="/">Craftify</a>
            <p class="opacity-90">Discover unique, handcrafted treasures from artisans around the globe. We bridge the gap between traditional craftsmanship and digital convenience.</p>
            <div class="flex gap-4">
                <a id="footerShareButton" href="#" aria-label="Share Craftify" class="w-8 h-8 rounded-full bg-surface-container-high flex items-center justify-center hover:bg-primary-container hover:text-on-primary-container transition-colors"><span class="material-symbols-outlined text-sm">share</span></a>
                <a href="mailto:support@craftify.com?subject=Craftify%20Support" aria-label="Email Craftify support" class="w-8 h-8 rounded-full bg-surface-container-high flex items-center justify-center hover:bg-primary-container hover:text-on-primary-container transition-colors"><span class="material-symbols-outlined text-sm">mail</span></a>
            </div>
        </div>
        <!-- Column 2: Shop -->
        <div>
            <h4 class="text-on-surface font-semibold uppercase tracking-wider mb-6">Shop</h4>
            <ul class="space-y-4">
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/products">All Products</a></li>
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/products?featured=1">Featured Products</a></li>
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/artisans">Featured Artisans</a></li>
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/products?sort=newest">New Arrivals</a></li>
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/auctions">Auctions</a></li>
            </ul>
        </div>
        <!-- Column 3: Support -->
        <div>
            <h4 class="text-on-surface font-semibold uppercase tracking-wider mb-6">Support</h4>
            <ul class="space-y-4">
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/about">About Craftify</a></li>
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/faq">Help Center</a></li>
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/guidelines">Artisan Guidelines</a></li>
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/contact">Contact Us</a></li>
            </ul>
        </div>
        <!-- Column 4: Legal -->
        <div>
            <h4 class="text-on-surface font-semibold uppercase tracking-wider mb-6">Legal</h4>
            <ul class="space-y-4">
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/privacy">Privacy Policy</a></li>
                <li><a class="opacity-90 hover:opacity-100 hover:text-primary hover:underline underline-offset-8 transition-all" href="/terms">Terms of Service</a></li>
            </ul>
            <div class="mt-8 pt-8">
                <p class="text-xs opacity-70">© 2026 Craftify. Built for the Digital Atelier.</p>
            </div>
        </div>
    </div>
</footer>
<!-- CSRF Token (available for JS injection into forms) -->
<meta name="csrf-token" content="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
<!-- Bottom Navigation Bar (Mobile only) -->
<%
    const mobileNav = typeof currentPath === 'string' ? currentPath : '/';
    const mobileActiveClass = 'flex flex-col items-center justify-center bg-amber-100 text-amber-900 rounded-2xl px-5 py-2';
    const mobileInactiveClass = 'flex flex-col items-center justify-center text-slate-500 px-5 py-2';
    const isShopActive = mobileNav.startsWith('/products');
    const isAuctionActive = mobileNav.startsWith('/auctions') && !mobileNav.startsWith('/auctions/my-bids');
    const isOrderActive = mobileNav.startsWith('/orders');
    const isBidActive = mobileNav.startsWith('/auctions/my-bids');
    const isCartActive = mobileNav.startsWith('/cart');
    const isProfileActive = mobileNav.startsWith('/user') || mobileNav.startsWith('/auth');
    const isCustomer = typeof user !== 'undefined' && user && user.role === 'customer';
    const isArtisanOrAdmin = typeof user !== 'undefined' && user && (user.role === 'artisan' || user.role === 'admin');
%>
<% if (isCustomer) { %>
<nav class="md:hidden fixed bottom-0 left-0 w-full z-50 flex justify-around items-center px-4 pb-6 pt-3 bg-white/80 dark:bg-slate-900/80 backdrop-blur-xl shadow-[0px_-10px_40px_rgba(25,28,30,0.04)] ">
    <a class="<%= isShopActive ? mobileActiveClass : mobileInactiveClass %>" href="/products">
        <span class="material-symbols-outlined <%= isShopActive ? 'filled-icon' : '' %>">storefront</span>
        <span class="font-['Inter'] text-[10px] uppercase tracking-widest font-bold mt-1">Shop</span>
    </a>
    <a class="<%= isAuctionActive ? mobileActiveClass : mobileInactiveClass %>" href="/auctions">
        <span class="material-symbols-outlined <%= isAuctionActive ? 'filled-icon' : '' %>">gavel</span>
        <span class="font-['Inter'] text-[10px] uppercase tracking-widest font-bold mt-1">Auction</span>
    </a>
    <a class="<%= isOrderActive ? mobileActiveClass : mobileInactiveClass %>" href="/orders">
        <span class="material-symbols-outlined <%= isOrderActive ? 'filled-icon' : '' %>">package_2</span>
        <span class="font-['Inter'] text-[10px] uppercase tracking-widest font-bold mt-1">Orders</span>
    </a>
    <a class="<%= isBidActive ? mobileActiveClass : mobileInactiveClass %>" href="/auctions/my-bids">
        <span class="material-symbols-outlined <%= isBidActive ? 'filled-icon' : '' %>">bid_landscape</span>
        <span class="font-['Inter'] text-[10px] uppercase tracking-widest font-bold mt-1">My Bids</span>
    </a>
</nav>
<% } else if (!isArtisanOrAdmin) { %>
<nav class="md:hidden fixed bottom-0 left-0 w-full z-50 flex justify-around items-center px-4 pb-6 pt-3 bg-white/80 dark:bg-slate-900/80 backdrop-blur-xl shadow-[0px_-10px_40px_rgba(25,28,30,0.04)] ">
    <a class="<%= isShopActive ? mobileActiveClass : mobileInactiveClass %>" href="/products">
        <span class="material-symbols-outlined <%= isShopActive ? 'filled-icon' : '' %>">storefront</span>
        <span class="font-['Inter'] text-[10px] uppercase tracking-widest font-bold mt-1">Shop</span>
    </a>
    <a class="<%= isAuctionActive ? mobileActiveClass : mobileInactiveClass %>" href="/auctions">
        <span class="material-symbols-outlined <%= isAuctionActive ? 'filled-icon' : '' %>">gavel</span>
        <span class="font-['Inter'] text-[10px] uppercase tracking-widest font-bold mt-1">Auction</span>
    </a>
    <a class="<%= isCartActive ? mobileActiveClass : mobileInactiveClass %>" href="/cart">
        <span class="material-symbols-outlined <%= isCartActive ? 'filled-icon' : '' %>">shopping_bag</span>
        <span class="font-['Inter'] text-[10px] uppercase tracking-widest font-bold mt-1">Cart</span>
    </a>
    <a class="<%= isProfileActive ? mobileActiveClass : mobileInactiveClass %>" href="/auth/login">
        <span class="material-symbols-outlined <%= isProfileActive ? 'filled-icon' : '' %>">login</span>
        <span class="font-['Inter'] text-[10px] uppercase tracking-widest font-bold mt-1">Login</span>
    </a>
</nav>
<% } %>

<script>
    (function () {
        var shareButton = document.getElementById('footerShareButton');
        if (!shareButton) return;

        shareButton.addEventListener('click', async function (event) {
            event.preventDefault();
            var shareData = {
                title: 'Craftify',
                text: 'Discover handcrafted products and live artisan auctions on Craftify.',
                url: window.location.origin
            };

            try {
                if (navigator.share) {
                    await navigator.share(shareData);
                    return;
                }

                if (navigator.clipboard && navigator.clipboard.writeText) {
                    await navigator.clipboard.writeText(shareData.url);
                    if (typeof showToast === 'function') showToast('Craftify link copied to clipboard!', 'success');
                    return;
                }

                window.prompt('Copy this link:', shareData.url);
            } catch (err) {
                // User canceled share or browser blocked clipboard access.
            }
        });
    })();
</script>

<script src="/js/form-validation.js" defer></script>
<script src="/js/form-loading.js" defer></script>
<script src="/js/image-preview.js" defer></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>
<script src="/js/main.js"></script>

<% if (typeof extraScripts !== 'undefined') { %>
    <%- extraScripts %>
<% } %>
</body>
</html>
